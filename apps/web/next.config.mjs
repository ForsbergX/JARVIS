/** @type {import('next').NextConfig} */
const nextConfig = {
  // The floating "N" badge in the corner is Next.js's own dev-mode route/
  // build indicator, not an app component — this is the framework's own
  // switch to turn it off, not a CSS hide.
  devIndicators: false,
};

export default nextConfig;
